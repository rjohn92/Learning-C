#include <stdio.h>

int main() {
    int *a; // Declare a pointer to an integer
    int b; // Declare an integer variable
    int *c; // Declare another pointer to an integer

    return 0; // Return 0 to indicate successful execution of the program
}